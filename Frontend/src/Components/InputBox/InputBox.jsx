import "./InputBox.css";

function InputBox({type,placeholder}){
    return(
        // <input type={type} placeholder={placeholder} />
        <input type={type} placeholder={placeholder}/>
    )
}

export default InputBox;